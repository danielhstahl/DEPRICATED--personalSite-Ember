import { dynPartial } from '../../../helpers/dyn-partial';
import { module, test } from 'qunit';

module('Unit | Helper | dyn partial');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = dynPartial([42]);
  assert.ok(result);
});
